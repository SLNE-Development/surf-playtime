name: Publish to Maven & Create GitHub Release

on:
  push:
    branches:
      - version/**
  workflow_dispatch:

jobs:
  build:
    uses: SLNE-Development/surf-workflows/.github/workflows/build-publish-release-gradle.yml@master
    with:
      modules: "surf-playtime-api-*-all.jar;surf-playtime-paper-*-all.jar;surf-playtime-microservice-*-all.jar"
    secrets: inherit